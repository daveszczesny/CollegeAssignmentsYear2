To run the program ensure Bash is installed on your device

To run the program you can now run
    ./server.sh


On another terminal run
	./client.sh $id
$id being your user name

You can then run the following commands

add $id
post $id $msg
display $id

